#include<bits/stdc++.h>
using namespace std;
const int MOD = 1e9 + 7;
const int N = 2e6 + 7;
#define deb(x) cout << #x << "=" << x << "\n"
#define DEBUG(a) for(auto &x:a) cout << x << " "


int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while(t--) {
        int n,m;
        cin >> n >> m;
        while(m-- ){
            int x,y,r;
            cin >> x >> y >> r;
            for(int i = 0; i < 2*r; i++) {
                int p,e;
                cin >> p >> e;

            }
        }
    }


    return 0;

}